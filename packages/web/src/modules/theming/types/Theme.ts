export interface Theme {
    colors: {
        primary: string
        accent: string
    }
    fontColors: {
        normal: string
        muted: string
    }
    stateColors: {
        warning: string
        danger: string
    }
}
